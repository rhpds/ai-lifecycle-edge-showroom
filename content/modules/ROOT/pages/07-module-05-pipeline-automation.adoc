= Module 5: Pipeline Automation

This module explores the automated pipeline system that continuously retrains machine learning models. You'll review the pipeline infrastructure, execute a manual pipeline run, and configure scheduled automation to enable self-improving AI models.

== Learning objectives

By the end of this module, you will be able to:

* Understand Kubeflow Pipelines architecture on Red Hat OpenShift AI
* Import and execute model retraining pipelines manually
* Monitor pipeline execution stages in real-time
* Configure scheduled pipeline runs for continuous automation (every 10 minutes)
* Understand how scheduling enables self-improving edge AI systems
* Verify conditional deployment logic (only deploy improved models)

[#review-pipeline-server]
== Exercise 5.1: Review pipeline infrastructure

Kubeflow Pipelines provides workflow automation for machine learning. The pipeline server orchestrates the complete model lifecycle from data collection through deployment.

=== Understanding pipeline architecture

**Why Pipelines Matter:**

Machine learning workflows involve multiple steps that must execute in sequence:

1. **Data Collection** - Retrieve fresh telemetry from robots
2. **Data Preparation** - Process and normalize training datasets
3. **Model Training** - Train models using updated data
4. **Model Validation** - Compare new models against existing ones
5. **Conditional Deployment** - Deploy only if new models outperform

Pipelines automate this workflow, ensuring:

* **Consistency** - Same process every time
* **Auditability** - Complete execution history
* **Scalability** - Handles increasing data volumes
* **Reliability** - Automatic retries on failures

=== Review pipeline server deployment

. In the **Red Hat OpenShift AI (RHOAI) Dashboard**, navigate to the `ai-edge-project` project
. Click the **Pipelines** tab
. Verify the pipeline server is pre-configured and shows _Import pipeline_

image::07-module-05_import-pipeline.png[Pipeline server pre-configured,link=self,window=blank,width=700]

The pipeline server was automatically deployed when the project was created with pipeline support enabled.

=== Verify pipeline server configuration

. In the upper right corner, click on the **three vertical dots**
. In the drop down menu, select **Manage pipeline server configuration**
+

image::07-module-05_pipeline-server-config.png[Review pipeline server configuration,link=self,window=blank,width=700]
+
. Review the Pipeline server configuration
* **Access key** - `minio` (SNO MinIO user)
* **Secret key** - `minio123` (SNO MinIO password)
* **Endpoint** - `\http://minio-service.minio.svc.cluster.local:9000` (SNO MinIO endpoint)
* **Bucket** - `pipeline` (bucket to store pipeline artifacts)

=== Verify

✓ **Pipelines** tab is accessible in `ai-edge-project`

✓ Pipeline server shows as configured

✓ Pipeline server allows to import pipelines

✓ Can access pipeline server configuration 

[#execute-pipeline]
== Exercise 5.2: Execute retraining pipeline

The retraining pipeline automates the complete model lifecycle: data collection → preprocessing → training → validation → conditional deployment. Execute the pipeline manually to understand the automated workflow.

=== Understanding the retraining pipeline

The pipeline automates:

1. **Data Collection** - Retrieve fresh battery telemetry from InfluxDB on robots
2. **Data Preparation** - Process and normalize data for training
3. **Model Training** - Train Stress Detection and Time-to-Failure models in parallel
4. **Model Validation** - Compare new models against existing models using test data
5. **Conditional Deployment** - If new models outperform existing ones, upload to robot storage

This workflow ensures models stay accurate as robot battery behavior evolves.

=== Import the retraining pipeline

Instead of creating the pipeline from scratch, import a pre-built definition.

**Download Pipeline Definition:**

. Open the **JupyterLab** workbench (model-training)
. Navigate to `ai-lifecycle-edge-gitops/notebooks/pipelines/`
. Locate the `model-retraining.yaml` file
. **Right-click** on it, select **Download**
+
image::07-module-05_pipeline-download.png[JupyterLab right-click context menu showing Download option for model-retraining.yaml,link=self,window=blank,width=700]

+
. Save the file to your laptop

**Import Pipeline:**

. Switch to the **RHOAI Dashboard**
. Navigate to the **Pipelines** tab in `ai-edge-project`
. Click **Import pipeline**
. Fill in the form:
+
* **Project**: `ai-edge-project` (pre-selected)
* **Pipeline name**: `Model retraining`
* **Upload a file**: Ensure this option is checked
* Click **Upload**, select the `model-retraining.yaml` file from your laptop

. Click **Import pipeline**

The pipeline graph displays, showing nodes and their connections.

image::07-module-05_pipeline-imported.png[Pipeline graph view showing data retrieval, preparation, parallel training, validation, and deployment nodes,link=self,window=blank,width=700]

=== Create a pipeline run

Execute the pipeline manually to test the automation workflow.

. Click the blue **Actions** button (top-right corner of pipeline graph)
. Select **Create run** from dropdown

. Configure the pipeline run:
+
* **Project**: `ai-edge-project` (verify selected)
* **Experiment**: `Default` (keep default)
* **Name**: `First run`
* **Pipeline**: `Model retraining` (pre-selected)
* **Pipeline version**: `Model retraining` (pre-filled)

. Review pipeline parameters (scroll down):
+
[cols="2,3",options="header"]
|===
| Parameter | Value

| `aws_access_key_id`
| `minio`

| `aws_s3_bucket`
| `inference`

| `aws_s3_endpoint`
| `http://minio-microshift-vm.microshift-001.svc.cluster.local:30000`

| `aws_secret_access_key`
| `minio123`

| `influxdb_bucket`
| `bms`

| `influxdb_org`
| `redhat`

| `influxdb_token`
| `admin_token`

| `influxdb_url`
| `https://influx-db-microshift-001.{openshift_cluster_ingress_domain}`
|===
+
NOTE: Update `influxdb_url` if your environment uses a different domain.
+
. Click **Create run**

=== Monitor pipeline execution

Watch the pipeline execute in real-time.

**Pipeline Stages:**

1. **Data Retrieval** - Fetches battery telemetry from InfluxDB on robots
2. **Data Processing** - Prepares and normalizes data
3. **Parallel Training** - Both models train simultaneously
4. **Model Validation** - New models evaluated against existing models
5. **Conditional Deployment** - If new models outperform, upload to MinIO

Wait for all nodes to complete.

image::07-module-05_pipeline-complete.png[Completed pipeline run with all nodes showing green checkmarks,link=self,window=blank,width=700]

=== Check new models

If new models outperformed existing ones, verify they were uploaded to MinIO:

. Open MinIO dashboard: `https://minio-microshift-001.{openshift_cluster_ingress_domain}`
. Login: `minio` / `minio123`
. Navigate to `inference` bucket
. Check model files in `stress-detection/1/` and `time-to-failure/1/`
. File timestamps show recent updates (within last few minutes)

=== Verify

✓ All nodes show **green checkmarks** (successful completion)

✓ Pipeline run shows "Succeeded" status in the Runs tab

✓ Run duration is reasonable (typically ~3-5 minutes)

[#schedule-automation]
== Exercise 5.3: Schedule pipeline automation

Scheduled pipelines enable fully automated model retraining. Configure the pipeline to run every 10 minutes, ensuring models continuously improve without manual intervention.

=== Why scheduled automation matters

Machine learning models drift over time as operational conditions change:

* **Battery aging** - Performance degrades as batteries wear
* **Temperature variations** - Seasonal changes affect battery behavior
* **Workload patterns** - Different operational routes stress batteries differently

**Scheduled Retraining Benefits:**

* **Eliminates manual intervention** - No human involvement required
* **Reduces model drift** - Models stay accurate with fresh data
* **Ensures reliability** - Predictive accuracy maintained over time
* **Enables true edge AI** - Autonomous systems that self-improve

For our robot fleet operating 24/7, automation is essential.

=== Create a scheduled pipeline run

Configure the pipeline to execute automatically every 10 minutes.

. In the **Red Hat OpenShift AI (RHOAI) Dashboard**, navigate to the **Pipeline definitions** tab
. Locate the `Model Retraining` pipeline
. Click the **three dots** (⋮) at the far right of the pipeline row
+
image::07-module-05_pipeline-scheduled.png[Pipeline list with three-dot menu for Model Retraining pipeline,link=self,window=blank,width=700]

+
. Select **Create schedule** from the dropdown

. Fill in the schedule configuration:
+
* **Name**: `Scheduled run`
* **Trigger type**: Select **Periodic**
* **Run every**: `10` `minutes`

. Scroll down to verify the **Model Retraining** pipeline is automatically selected

. Review parameters (same as manual run):
+
[cols="2,3",options="header"]
|===
| Parameter | Value

| `aws_access_key_id`
| `minio`

| `aws_s3_bucket`
| `inference`

| `aws_s3_endpoint`
| `http://minio-microshift-vm.microshift-001.svc.cluster.local:30000`

| `aws_secret_access_key`
| `minio123`

| `influxdb_bucket`
| `bms`

| `influxdb_org`
| `redhat`

| `influxdb_token`
| `admin_token`

| `influxdb_url`
| `https://influx-db-microshift-001.{openshift_cluster_ingress_domain}`
|===
+
. Click **Create schedule**

Every 10 minutes, the scheduled pipeline:

. **Retrieves** - Fresh battery telemetry from factory robots (last hour of data)
. **Processes** - Normalizes and prepares training datasets
. **Trains** - New Stress Detection and Time-to-Failure models
. **Validates** - Compares new models against existing models
. **Deploys** - Uploads improved models to robot storage (if they outperform)
. **Repeats** - Cycle continues every 10 minutes

This creates a **self-improving system** where models get better over time without human intervention.

=== Verify

✓ Schedule can be created

✓ **Run every** is set to `10 minutes`

✓ Run executes and completes successfully

[#summary]
== Summary

You have configured fully automated pipeline scheduling for continuous model retraining:

**✓ Pipeline Infrastructure** - Reviewed pipeline server deployment and configuration

**✓ Pipeline Import** - Loaded Model retraining pipeline definition from YAML

**✓ Manual Execution** - Ran pipeline to test all stages (data → training → validation → deployment)

**✓ Scheduled Automation** - Configured periodic runs every 10 minutes

**✓ Continuous Improvement** - Verified automated model retraining workflow

**What You've Learned:**

* Kubeflow Pipelines architecture on Red Hat OpenShift AI
* How to import and execute ML pipelines
* Real-time pipeline monitoring and log analysis
* Scheduled pipeline configuration for automation
* Conditional deployment logic to prevent model degradation
* Complete MLOps workflow from training to deployment

Next, you'll explore the alerting and monitoring system to ensure automated models remain fair and accurate over time.

Navigate to xref:08-module-06-test-alerts.adoc[Module 6: Test Alerts].
