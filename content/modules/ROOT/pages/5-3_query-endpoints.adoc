= Module 4.3: Query Model Endpoints

Test the model inference endpoints to verify they respond correctly to prediction requests. You'll use a Jupyter notebook to send test data and receive predictions.

[#understanding-inference-requests]
== Understanding inference POST requests

Model inference uses HTTP POST requests with JSON payloads following the KServe v2 protocol.

**Endpoint Format:**

[source,text]
----
POST http://<service-name>.ai-edge-project.svc.cluster.local:8888/v2/models/<model-name>/infer
----

**Request Body Structure:**

[source,json]
----
{
  "inputs": [
    {
      "name": "keras_tensor",       // Input tensor name (from model)
      "shape": [1, N],               // Batch size 1, N features
      "datatype": "FP32",            // 32-bit floating point
      "data": [...]                  // Feature values (normalized)
    }
  ]
}
----

**Input Features:**

**Stress Detection (9 features):**

* Voltage, Temperature, State of Charge, Discharge Rate
* Time Elapsed, Cycle Count, Ambient Temperature
* Usage Pattern, Health Status

**Time to Failure (5 features):**

* Temperature Delta, Voltage Delta, Resistance
* Current Delta, Capacity Fade

**Response Structure:**

[source,json]
----
{
  "outputs": [
    {
      "name": "output",
      "shape": [1, 1],
      "datatype": "FP32",
      "data": [0.87]              // Prediction (stress score or hours)
    }
  ]
}
----

For Stress Detection: values > 0.5 indicate STRESSED, ≤ 0.5 indicate NORMAL

For Time to Failure: value represents predicted hours until battery failure

[#access-query-notebook]
== Task 1: Access the Query Notebook

1. Return to the **JupyterLab** environment (model-training workbench)
2. Navigate to `ai-lifecycle-edge-gitops/notebooks/serving/`
3. Open the `query_models.ipynb` notebook

IMAGE_HERE: JupyterLab showing query_models.ipynb notebook in the serving folder

=== Verify

Confirm the query notebook is accessible:

✓ JupyterLab workbench is open
✓ Navigate to `ai-lifecycle-edge-gitops/notebooks/serving/` folder
✓ `query_models.ipynb` notebook exists
✓ Notebook opens successfully with Python kernel

[#query-stress-detection]
== Task 2: Query Stress Detection Model

The notebook contains code to test the Stress Detection endpoint.

**Verify the endpoint URL in the first cell:**

[source,python]
----
stress_detection_url = "http://stress-detection-predictor.ai-edge-project.svc.cluster.local:8888"
----

**Execute the cells to:**

1. Define the inference endpoint
2. Prepare test input data (9 normalized feature values)
3. Send POST request with test data
4. Receive and display prediction

**Expected Response:**

The model returns a stress score between 0 and 1. The notebook interprets:

* Score > 0.5 → **STRESSED**
* Score ≤ 0.5 → **NORMAL**

=== Verify

Confirm Stress Detection inference works:

✓ Endpoint URL is correct (`stress-detection-predictor.ai-edge-project.svc.cluster.local:8888`)
✓ Test data contains 9 feature values (normalized)
✓ POST request executes without errors
✓ Response contains a prediction score between 0 and 1
✓ Notebook interprets and displays stress status (NORMAL or STRESSED)

**Example successful output:**
----
Stress Score: 0.73
Status: STRESSED ⚠️
----

[#query-time-to-failure]
== Task 3: Query Time to Failure Model

Scroll down in the notebook to the Time to Failure section.

**Verify the endpoint URL:**

[source,python]
----
time_to_failure_url = "http://time-to-failure-predictor.ai-edge-project.svc.cluster.local:8888"
----

**Execute the cells to:**

1. Define the TTF inference endpoint
2. Prepare test input data (5 feature values)
3. Send POST request
4. Receive and display time-to-failure prediction

**Expected Response:**

The model returns the predicted number of hours until battery failure (e.g., "Predicted Time Before Failure: 42.5 hours").

=== Verify

Confirm Time to Failure inference works:

✓ Endpoint URL is correct (`time-to-failure-predictor.ai-edge-project.svc.cluster.local:8888`)
✓ Test data contains 5 feature values (delta metrics)
✓ POST request executes without errors
✓ Response contains a prediction (hours until failure)
✓ Prediction value is reasonable (not negative, not NaN)

**Example successful output:**
----
Predicted Time Before Failure: 42.5 hours
Battery should be replaced within: 1.8 days
----

[#verify-both-models]
== Task 4: Verify Both Models Respond

After executing all cells in the notebook, confirm:

=== Verify

Confirm both model endpoints are operational:

✓ **Stress Detection endpoint** responds successfully
✓ **Time to Failure endpoint** responds successfully
✓ No HTTP connection errors (404, 500, timeout)
✓ Both predictions return valid numeric values
✓ Stress score is between 0 and 1
✓ TTF prediction is a positive number (hours)
✓ JSON response structure matches KServe v2 format
✓ Notebook executes all cells without errors

**If issues occur:**
- Verify model server pods are `Running` in `ai-edge-project` namespace
- Check endpoint URLs match pod service names
- Confirm workbench can reach internal cluster DNS (`.svc.cluster.local`)

Both models are now validated and ready for use in pipelines and automation.

[#summary]
== Summary: Module 4 Complete

You have successfully reviewed and tested the model serving infrastructure:

**Module 4.1 - Stress Detection Server:**

* Reviewed OpenVINO Model Server configuration
* Examined ServingRuntime and InferenceService resources
* Verified model endpoint availability

**Module 4.2 - Time to Failure Server:**

* Reviewed TTF model server configuration
* Compared two complementary models
* Verified separate model paths and endpoints

**Module 4.3 - Query Endpoints:**

* Understood KServe v2 inference POST request format
* Tested Stress Detection endpoint with sample data
* Tested Time to Failure endpoint with sample data
* Verified both models respond correctly

**What You've Learned:**

* KServe v2 protocol for REST API inference
* JSON request/response structure for model queries
* How to format input features for each model
* Interpreting model outputs (stress scores, time estimates)
* Using Jupyter notebooks to test inference endpoints
* Internal Kubernetes service DNS for endpoint access

**The model serving infrastructure is fully operational and validated.**

Next, you'll explore the automated pipeline system that retrains models every 10 minutes using fresh data from the robot fleet.

Navigate to link:./6-1_pipeline-server.html[Module 5.1: Review Pipeline Server].
