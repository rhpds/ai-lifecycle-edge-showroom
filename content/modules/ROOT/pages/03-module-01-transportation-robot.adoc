= Module 1: Transportation Robot

This module explores the complete infrastructure running on Red Bot Logistics' autonomous transportation robots. Each robot runs **Red Hat Device Edge (MicroShift)**, a lightweight Kubernetes platform optimized for resource-constrained environments.

== Learning objectives

By the end of this module, you will be able to:

* Connect to robot infrastructure via bastion host and verify MicroShift is operational
* Deploy MinIO object storage using GitOps automation
* Deploy AI model serving infrastructure with KServe
* Deploy and access the Battery Monitoring System dashboard

[#connect-robot]
== Exercise 1.1: Robot infrastructure setup

Access the robot infrastructure and verify MicroShift is running.

=== Connect to bastion host

Establish an SSH connection to the bastion host:

[source,bash,subs="attributes+"]
----
{bastion_ssh_command}
----

[cols="1,2"]
|===
| Credential | Value

| Password
| `{bastion_ssh_password}`
|===

=== Connect to robot VM

Once connected to the bastion, access the transportation robot's MicroShift VM:

[source,bash,subs="attributes+"]
----
virtctl ssh cloud-user@vmi/microshift -n microshift-001 --identity-file=/home/lab-user/.ssh/microshift-001
----

=== Verify MicroShift is running

MicroShift is a lightweight Kubernetes distribution designed for edge devices. It provides:

* **Minimal footprint** - Runs with ~2 CPU cores and 2GB RAM
* **Kubernetes API** - Full compatibility with standard Kubernetes tools
* **Edge-optimized** - Built for resource-constrained, distributed deployments
* **Embedded capabilities** - Model serving and GitOps built-in

Check that MicroShift is running:

[source,bash]
----
sudo systemctl status microshift
----

Expected output should show `active (running)`.

Verify cluster access:

[source,bash]
----
oc get nodes
----

You should see 1 node in `Ready` status.

=== Explore deployed namespaces

List the namespaces already deployed on the robot:

[source,bash]
----
oc get namespaces
----

You should see several namespaces including:

* `openshift-gitops` - GitOps for automated deployments
* `redhat-ods-applications` - Model serving components

NOTE: Some namespaces may not exist yet. You'll deploy these components using GitOps in the following exercises.

=== Verify

Confirm you are connected and MicroShift is operational:

**Expected hostname:**
----
microshift
----

**Expected systemctl output:**
----
● microshift.service - MicroShift
     Loaded: loaded (/usr/lib/systemd/system/microshift.service; enabled)
     Active: active (running) since ...
----

**Expected nodes output:**
----
NAME                    STATUS   ROLES                         AGE   VERSION
localhost.localdomain   Ready    control-plane,master,worker   5d    v1.32.6
----

✓ Hostname shows `microshift`

✓ MicroShift service shows `active (running)`

✓ Node status is `Ready`

✓ Kubernetes API is accessible via `oc` command

[#deploy-minio]
== Exercise 1.2: MinIO storage deployment

MinIO provides S3-compatible object storage for AI models on the robot. The `inference` bucket stores two AI models: **Stress Detection** (identifies battery stress conditions) and **Time to Failure** (predicts remaining battery life).

Both models are stored in OpenVINO IR format (`.xml` and `.bin` files) in versioned directories.

=== Create GitOps application project

Create the default project for GitOps applications:

[source,yaml]
----
oc apply -f - <<EOF
kind: AppProject
apiVersion: argoproj.io/v1alpha1
metadata:
  name: default
  namespace: openshift-gitops
spec:
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
  destinations:
  - namespace: '*'
    server: '*'
  sourceRepos:
  - '*'
EOF
----

=== Deploy MinIO using GitOps

Deploy MinIO by creating a GitOps Application:

[source,yaml]
----
oc apply -f - <<EOF
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: minio-microshift
  namespace: openshift-gitops
spec:
  destination:
    server: https://kubernetes.default.svc
  source:
    path: bootstrap/minio-microshift/groups/dev
    repoURL: https://github.com/rhpds/ai-lifecycle-edge-gitops.git
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
EOF
----

This deploys:

* **Namespace** - `minio` namespace for isolation
* **Deployment** - MinIO server instance
* **Service & Route** - Web dashboard access
* **PersistentVolumeClaim** - Storage for model files
* **Bucket creation** - Automatically creates `inference` bucket
* **Model download** - Downloads baseline Stress Detection and Time to Failure models from GitHub

Watch the deployment:

[source,bash]
----
watch oc get pods -n minio
----

Press `Ctrl+C` to exit when pods show `Running` and `Completed` status.

=== Access MinIO dashboard

Once deployed, access the MinIO web console:

. Open the MinIO dashboard:
+
[source,bash,subs="attributes+"]
----
https://minio-microshift-001.{openshift_cluster_ingress_domain}
----

. Login with credentials:
+
* Username: `minio`
* Password: `minio123`

. Verify the `inference` bucket contains two directories:
+
* `stress-detection/1/` - Baseline Stress Detection model files (`.xml`, `.bin`)
* `time-to-failure/1/` - Baseline Time to Failure model files (`.xml`, `.bin`)

image::2-2_minio-inference.png[MinIO dashboard showing inference bucket with model directories,link=self,window=blank,width=700]

=== Verify

Navigate through the MinIO dashboard:

1. Click on the **inference** bucket
2. Verify you see two folders: `stress-detection/` and `time-to-failure/`
3. Click into `stress-detection/1/` - verify you see `.xml` and `.bin` files
4. Go back and click into `time-to-failure/1/` - verify you see `.xml` and `.bin` files

Expected files in each model directory:
----
stress-detection/1/
  ├── model.xml          (OpenVINO model definition)
  └── model.bin          (OpenVINO model weights)

time-to-failure/1/
  ├── model.xml
  └── model.bin
----

✓ GitOps application shows `Synced` and `Healthy`

✓ MinIO pod shows `Running` with `1/1` ready

✓ `inference` bucket exists with both model directories

✓ Each directory contains `.xml` and `.bin` files

[#deploy-model-serving]
== Exercise 1.3: Model serving deployment

The inference server loads AI models from MinIO and exposes REST API endpoints for real-time predictions. It uses KServe (part of Red Hat OpenShift AI) optimized for edge deployments.

**Model Serving Architecture:**

* **ServingRuntime:** Defines the model server container (OpenVINO Model Server)
* **InferenceService:** Connects models from MinIO to the ServingRuntime
* **Service Account:** Provides S3 credentials to access MinIO storage
* **Routes:** Expose inference endpoints externally for API access

=== Deploy model serving using GitOps

Deploy the inference server GitOps application:

[source,yaml]
----
oc apply -f - <<EOF
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: model-server
  namespace: openshift-gitops
spec:
  destination:
    server: https://kubernetes.default.svc
  source:
    path: bootstrap/model-server/groups/dev
    repoURL: https://github.com/rhpds/ai-lifecycle-edge-gitops.git
    targetRevision: main
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
EOF
----

This deploys:

* **Namespace** - `inference` namespace for model serving
* **ServingRuntime** - OpenVINO Model Server with REST and gRPC support
* **Service & Routes** - External access to inference endpoints
* **InferenceServices** (2):
** `stress-detection` - Loads stress detection model from MinIO
** `time-to-failure` - Loads time-to-failure model from MinIO

Watch the deployment:

[source,bash]
----
watch oc get pods -n inference
----

Press `Ctrl+C` to exit when all pods show `Running` status.

=== Test inference endpoints

Verify that models are loaded and responding to inference requests.

**Test stress detection model:**

[source,bash]
----
curl -s -X POST http://$(oc get pod -n inference -l app=isvc.stress-detection-predictor -o jsonpath='{.items[0].status.podIP}'):8888/v2/models/stress-detection/infer \
     -H "Content-Type: application/json" \
     -d '{"inputs": [{"name": "keras_tensor", "shape": [1, 9], "datatype": "FP32", "data": [0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5]}]}' | \
     jq -r '.outputs[0].data[0] | if . > 0.5 then "STRESSED" else "NORMAL" end'
----

Expected output: `STRESSED` or `NORMAL`

**Test time to failure model:**

[source,bash]
----
curl -s -X POST http://$(oc get pod -n inference -l app=isvc.time-to-failure-predictor -o jsonpath='{.items[0].status.podIP}'):8888/v2/models/time-to-failure/infer \
     -H "Content-Type: application/json" \
     -d '{"inputs": [{"name": "keras_tensor", "shape": [1, 5], "datatype": "FP32", "data": [40.0, 48.0, 162.5, -2.0, -18.0]}]}' | \
     jq -r '.outputs[0].data[0] | "Predicted Time Before Failure: \(.) hours"'
----

Expected output: `Predicted Time Before Failure: <number> hours`

=== Verify

Check that all model serving pods are running:

[source,bash]
----
oc get pods -n inference
----

Expected output:
----
NAME                                                      READY   STATUS    RESTARTS   AGE
stress-detection-predictor-xxxxx-xxxxx                    2/2     Running   0          3m
time-to-failure-predictor-xxxxx-xxxxx                     2/2     Running   0          3m
----

Verify InferenceServices are ready:

[source,bash]
----
oc get inferenceservice -n inference
----

Expected output:
----
NAME                URL                                                        READY
stress-detection    http://stress-detection.inference.svc.cluster.local        True
time-to-failure     http://time-to-failure.inference.svc.cluster.local         True
----

✓ Both predictor pods show `2/2` ready and `Running` status

✓ Both InferenceServices show `READY = True`

✓ Models respond to test inference requests

✓ Stress detection model returns `STRESSED` or `NORMAL`

✓ Time to failure model returns predicted hours

[#deploy-bms]
== Exercise 1.4: Battery monitoring system deployment

The Battery Monitoring System (BMS) brings together all robot components to provide real-time battery health monitoring. It collects telemetry data, displays it on a dashboard, and uses the AI inference endpoints to generate predictions.

**BMS Architecture:**

* **Battery Simulator** → **MQTT Broker** → **Data Processors** → **InfluxDB** + **Dashboard**
* **Dashboard** → **Inference APIs** → **AI Predictions** → **Alerting**

=== Deploy BMS using GitOps

Deploy the complete Battery Monitoring System application:

[source,yaml,subs="attributes+"]
----
oc apply -f - <<EOF
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: battery-simulation
  namespace: openshift-gitops
spec:
  destination:
    server: https://kubernetes.default.svc
    namespace: battery-demo
  source:
    path: bootstrap/battery-simulation/groups/dev
    repoURL: https://github.com/rhpds/ai-lifecycle-edge-gitops.git
    targetRevision: main
    kustomize:
      patches:
        - target:
            kind: ConfigMap
            name: bms-dashboard-config
          patch: |-
            - op: replace
              path: /data/config.json
              value: |
                {
                  "BATTERY_METRICS_WS_ENDPOINT": "wss://mqtt2ws-microshift-001.{openshift_cluster_ingress_domain}/battery/metrics/",
                  "OPEN_AI_API_KEY": "my-key",
                  "STRESS_DETECTION_AI_API_ENDPOINT": "https://stress-detection-predictor-microshift-001.{openshift_cluster_ingress_domain}/",
                  "TIME_TO_FAILURE_AI_API_ENDPOINT": "https://time-to-failure-predictor-microshift-001.{openshift_cluster_ingress_domain}/",
                  "BATTERY_SIMULATION_API_ENDPOINT": "https://battery-simulation-microshift-001.{openshift_cluster_ingress_domain}/",
                  "AUTO_INFERENCE_INTERVAL_MS": 30000
                }
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
EOF
----

This deploys:

* **Battery Simulator** - Quarkus app generating realistic telemetry
* **MQTT Broker** - Mosquitto broker as central messaging hub
* **Data Processors** - `mqtt2ws` (WebSocket) and `data-ingester` (InfluxDB)
* **InfluxDB** - Time-series database for historical sensor data
* **BMS Dashboard** - Web UI showing real-time telemetry and AI predictions

Monitor the deployment:

[source,bash]
----
watch oc get pods -n battery-demo
----

Press `Ctrl+C` to exit when all pods show `Running` status.

=== Access the BMS dashboard

Open the Battery Monitoring System dashboard:

[source,bash,subs="attributes+"]
----
https://bms-dashboard-microshift-001.{openshift_cluster_ingress_domain}
----

The dashboard displays:

* **Real-time telemetry** - Voltage, temperature, charge level, operational metrics
* **AI predictions** - Stress detection status (NORMAL/STRESSED)
* **Time-to-failure estimates** - Predicted hours until battery failure
* **Alerts** - Notifications when stress is detected or failure is imminent

image::2-4_bms-dashboard.png[BMS dashboard showing real-time telemetry graphs and AI prediction indicators,link=self,window=blank,width=700]

=== Verify data in InfluxDB

Access the InfluxDB dashboard to see historical sensor data:

. Open the InfluxDB dashboard:
+
[source,bash,subs="attributes+"]
----
https://influx-db-microshift-001.{openshift_cluster_ingress_domain}
----

. Login with credentials:
+
* Username: `admin`
* Password: `password`

. Navigate to **Explore** in the left menu

. Query battery data:
+
.. Select the `bms` bucket from the dropdown
.. Choose `battery_data` measurement
.. Click **Submit** to execute the query

Adjust the time interval (e.g., "Past 5 minutes") to see recent data.

image::2-4_influxdb-telemetry.png[InfluxDB dashboard showing battery telemetry time-series data,link=self,window=blank,width=700]

=== Verify

Check that all BMS pods are running:

[source,bash]
----
oc get pods -n battery-demo
----

Expected output:
----
NAME                                  READY   STATUS    RESTARTS   AGE
battery-simulation-xxxxx-xxxxx        1/1     Running   0          2m
bms-dashboard-xxxxx-xxxxx             1/1     Running   0          2m
data-ingester-xxxxx-xxxxx             1/1     Running   0          2m
influxdb-xxxxx-xxxxx                  1/1     Running   0          2m
mosquitto-mqtt-broker-xxxxx-xxxxx     1/1     Running   0          2m
mqtt2ws-xxxxx-xxxxx                   1/1     Running   0          2m
----

Verify dashboard functionality:

✓ All 6 BMS pods show `1/1` ready and `Running` status

✓ GitOps application shows `Synced` and `Healthy`

✓ BMS dashboard loads successfully in browser

✓ Telemetry charts show live updates (voltage, temperature, etc.)

✓ AI Prediction panel displays stress detection status

✓ Time to Failure shows estimated hours remaining

✓ InfluxDB contains `bms` bucket with `battery_data` measurement

✓ Time-series graphs display sensor values

[#troubleshooting]
== Troubleshooting

**Issue**: GitOps application stuck in "Progressing" state

**Solution**:

. Check application status: `oc describe application <app-name> -n openshift-gitops`
. Review sync errors in status section
. Verify Git repository is accessible
. Common fix: Delete and recreate the application

**Issue**: MinIO pods in "CrashLoopBackOff" state

**Solution**:

. Check pod logs: `oc logs <pod-name> -n minio`
. Verify PersistentVolumeClaim is bound: `oc get pvc -n minio`
. Check storage class availability: `oc get storageclass`
. If PVC pending, check node storage capacity

**Issue**: Inference pods fail to start with "ImagePullBackOff"

**Solution**:

. Verify pod status: `oc describe pod <pod-name> -n inference`
. Check image name in ServingRuntime configuration
. Ensure network connectivity for image registry
. Common fix: Wait for automatic retry or restart deployment

**Issue**: BMS dashboard shows "Connection Failed" or no data

**Solution**:

. Verify all battery-demo pods are running: `oc get pods -n battery-demo`
. Check WebSocket endpoint accessibility
. Verify inference endpoints are reachable from dashboard pod
. Test inference endpoints manually using curl commands from Exercise 1.3
. Check ConfigMap has correct endpoint URLs: `oc get cm bms-dashboard-config -n battery-demo -o yaml`

**Issue**: InfluxDB shows no data in query results

**Solution**:

. Verify data-ingester pod is running: `oc get pod -n battery-demo | grep data-ingester`
. Check data-ingester logs for errors: `oc logs -n battery-demo -l app=data-ingester`
. Verify MQTT broker is receiving data: `oc logs -n battery-demo -l app=mosquitto-mqtt-broker`
. Ensure battery-simulation pod is generating data: `oc logs -n battery-demo -l app=battery-simulation`

[#learning-outcomes]
== Learning outcomes

Before moving to the next module, verify you understand:

**Concepts you should now understand:**

* How MicroShift provides lightweight Kubernetes for edge devices with minimal resource footprint
* Why GitOps automation is critical for managing distributed edge deployments at scale
* How S3-compatible object storage (MinIO) enables AI model distribution to edge devices
* The architecture of KServe-based model serving and the KServe v2 inference protocol
* How MQTT messaging enables real-time data collection from IoT sensors
* The role of time-series databases (InfluxDB) in storing and analyzing sensor telemetry
* How AI inference integrates into monitoring dashboards to provide actionable insights

**Tasks you should be able to perform:**

* Connect to edge infrastructure via bastion host and SSH tunneling
* Deploy applications using GitOps (Argo CD) with Application custom resources
* Verify Kubernetes resource status (pods, services, routes, custom resources)
* Access and navigate S3-compatible object storage dashboards
* Test inference endpoints using curl and the KServe v2 protocol
* Interpret AI model predictions from REST API responses
* Access and query time-series data in InfluxDB
* Monitor real-time telemetry data in web-based dashboards

**Key questions to ask yourself:**

. What makes MicroShift suitable for edge deployments compared to full OpenShift? (Answer: Minimal footprint, single-node operation, embedded capabilities)
. How does GitOps ensure consistency across a fleet of edge devices? (Answer: Declarative configuration in Git, automated sync, self-healing)
. Why are models stored in versioned directories like `/stress-detection/1/`? (Answer: Enables model versioning, A/B testing, rollback capabilities)
. What is the purpose of the KServe v2 protocol? (Answer: Standardized inference API for model serving across different frameworks)
. How does the BMS dashboard obtain AI predictions? (Answer: Sends telemetry data to inference endpoints via HTTP POST, receives predictions in response)

If you can answer these questions confidently, you're ready to proceed to the next module!

[#summary]
== Summary

You have successfully deployed the complete transportation robot infrastructure on MicroShift. The robot now has:

**✓ MicroShift** - Lightweight Kubernetes platform running on RHEL {rhel_version}

**✓ MinIO Storage** - S3-compatible storage with AI models in the `inference` bucket

**✓ Model Serving** - OpenVINO inference servers for Stress Detection and Time to Failure models

**✓ Battery Monitoring System** - Complete telemetry collection, visualization, and AI prediction pipeline

**What You've Learned:**

* How to access edge infrastructure via bastion host
* GitOps-based deployment for edge automation
* S3 object storage for AI model distribution
* KServe model serving architecture on edge devices
* Inference API request/response format (KServe v2 protocol)
* Real-time telemetry collection and time-series storage
* Integration of AI predictions into monitoring dashboards

**The robot is now operational with real-time battery health monitoring.**

Next, you'll explore the **Single Node OpenShift (SNO) retraining server** where AI models are automatically retrained every 10 minutes using fresh data from the robot fleet.

Navigate to xref:04-module-02-rhoai-configuration.adoc[Module 2: RHOAI Configuration].
